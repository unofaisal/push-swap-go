init function must
 -set current postion for both list
 -set price for pushing to a ie setprice value for list b
 -set cheapest(b)


first check if node len == 3 and perform tinysort
    this needs helper func to check for list_max_node()
    if l[1] == max_node do ra
    if l[2] == max_node do rra

    then check between the first and and do sa if needed


set_target_node:
    loop over all nodes in stackb 
        check for the target